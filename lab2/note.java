Git

commend lane ... 

$ git config -- global user.name " darecall"

$ git config -- global user.email "Ashane199080706@hotmail.com.tw"


$ git status

pwd show the address

touch .gitignore ( create a file .gitignore) tell it to ingore everything

in this file

git rm [options] -- <file>...

git.ssh  ( ssh key )

ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
// assign a ssh key
after that  eval "$(ssh-agent -s)"

check ssh key  (Agent pid 2537)

test your ssh commend

git add .

---- >  git commit -m " string"

git branch dev   then git checkout 'dev'

git long -- graph -- decorate -- online -- all

checkout commend " managing file system"
// git checkout "file name"

git merge "file"   // merge curr file with files

